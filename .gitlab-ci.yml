# Gitlab CI docket image
image: node:6.6.0

stages:
  - build
  - test
  - deploy

cache:
  paths:
    - node_modules/


build:
  stage: build
  script:
    - npm install
  tags:
    - docker

test:
  stage: test
  script:
    - ./bin/test
  tags:
    - docker

deploy:
  stage: deploy
